import styled from '@emotion/styled';

export const InputDefault = styled.input`
  padding: 0 12px;
  font-weight: 500;
  border-radius: 4px;
  font-size: 18px;
  color: #2c3e50;

  &:hover {
    box-shadow: none;
    transform: none;
    background: whitesmoke;
  }
`;
